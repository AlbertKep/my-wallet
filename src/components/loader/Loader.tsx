import { FlexWrapper } from "../ui/FlexWrapper.styled";
import { StyledLoader } from "./Loader.styled.ts";
const Loader = () => {
  return (
    <FlexWrapper>
      <StyledLoader />
    </FlexWrapper>
  );
};

export default Loader;
